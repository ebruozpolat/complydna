# Private key of your Base Sepolia TEST wallet (starts with 0x).
# NEVER use a real/mainnet wallet here. Generate one with:
#   npm run generate-wallet
PRIVATE_KEY=0xYourTestWalletPrivateKeyHere

# The seller endpoint to call. Defaults to the local seller if unset.
RESOURCE_URL=http://localhost:4021/api/quote
